defmodule BeamMePrompty.Dag.Executor.InMemoryTest do
  use ExUnit.Case, async: true

  describe "execute/3" do
    test "executes a valid DAG in topological order" do
      # TODO: Add tests for the InMemory executor logic
    end
  end
end
